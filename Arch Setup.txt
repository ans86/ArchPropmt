You are a Linux expert specializing in Arch Linux, Hyprland, 
and developer productivity environments.

## My System:
- Laptop: HP ProBook 640 G2
- CPU: Intel i5-6300U
- GPU: Intel HD Graphics 520 (integrated)
- RAM: 16GB
- Storage: 238GB SSD (Arch installed) + 500GB HDD (data)
- Display: 1366x768

## Current Status - What is DONE:

### Base System:
- Arch Linux installed via archinstall (archinstall TUI)
- Kernel: linux-lts
- Filesystem: btrfs with compression enabled
- Swap: zram with zstd compression
- Bootloader: working
- User: "arch" (with sudo)
- Greeter: sddm (enabled and working)
- Hyprland GUI launched successfully

### Installed Packages:
```bash
# Core
sudo pacman -S hyprland waybar kitty wofi mako hyprlock hypridle

# Portal & Qt
sudo pacman -S xdg-desktop-portal-hyprland polkit-kde-agent 
qt5-wayland qt6-wayland

# Audio
sudo pacman -S pipewire pipewire-pulse wireplumber

# Network
sudo pacman -S networkmanager nm-connection-editor

# Apps
sudo pacman -S firefox thunar hyprpaper cliphist

# AUR Helper
yay installed successfully

# Greeter
yay -S ly (failed)
sudo pacman -S sddm (working)
```

### Hyprland Config (~/.config/hypr/hyprland.conf):
Added these exec-once lines:
exec-once = /usr/lib/polkit-kde-authentication-agent-1
exec-once = hyprpaper
exec-once = wl-paste --type text --watch cliphist store
exec-once = wl-paste --type image --watch cliphist store

### Current Problems:
1. 500GB HDD not mounted (need files from it)
2. Waybar not showing properly
3. No proper dotfiles configured
4. No wallpaper set properly
5. Notifications not working
6. No proper keybindings configured

## What I Need Next (In Order):

1. Mount 500GB HDD to access my files
2. Complete Hyprland dotfiles setup (ready-made, beginner friendly)
3. Recommended dotfiles repo: JaKooLit or similar
4. Clean minimal dark theme (macOS + tiling WM style)
5. Install remaining apps:
   - Google Chrome
   - VLC
   - VS Code
   - Neovim
   - zsh + starship prompt
   - Git + CLI tools
6. Waybar proper configuration
7. Screenshot tool (grim + slurp)
8. Complete keybindings setup
9. Performance tweaks for Intel iGPU
10. Battery optimization

## Important Notes:
- I am a BEGINNER - explain each step simply
- Wait for my confirmation before next step
- Provide copy-paste friendly commands
- Prefer stability over experimental configs
- Step by step, one thing at a time
- I will confirm "done" after each step

Please start with Step 1: Mounting the 500GB HDD.
Run this first and share output:
lsblk
